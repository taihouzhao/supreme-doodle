{
  "name": "korea-tactics-slice",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "可验证的战棋纵向切片：确定性规则核心 + 网页表现 + 蒙特卡洛模拟器",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "sim": "tsx src/sim/cli.ts"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3",
    "vite": "^6.0.11",
    "vitest": "^3.0.5"
  }
}

---
description: Gameplay balance sim + deploy standing orders
alwaysApply: true
---

# 玩法变更与发布站规

## 1. 蒙特卡洛打磨（玩法变更必做）

凡是**影响游戏玩法 / 数值 / 关卡难度 / AI 行为**的变更，不得只靠手感或单局试玩收工：

1. 用蒙特卡洛工具反复模拟：`npm run sim`，深入调参用 `npm run balance:tune`
2. 目标：以**基础策略（玩家代理）的战役三关全胜率**落在约 **胜 6 负 4**（默认带 **52%–68%**，中心 **60%**）
3. 同时仍须通过既有能力梯度门槛（随机 ≪ 基础 ≤ 战术、无死档种子、无统治性退化打法、重创可恢复等）
4. 调参写回 `src/content/balance.ts`，并更新 `reports/balance.md`
5. 门槛未通过时，不得把该玩法变更当作完成

纯表现层（贴图、动画、文案、布局）可不跑完整调参，但若顺带改了规则数值则必须跑。

## 2. 变更后直接部署（方便试玩）

玩法或可感知内容变更完成后：

1. **提交并推送**当前工作分支（`cursor/*-eb04`）
2. GitHub Actions **Deploy to R2** 会对 `cursor/**` 与 `main` 自动构建上传
3. 试玩地址：https://korea-tactics.dashjie.net/index.html （强制刷新）
4. 可用 `npm run ship` 一键：模拟门槛 → 构建检查 → 提醒推送部署

不要把「只开了 PR、没推送/没进部署白名单」当成已上线。

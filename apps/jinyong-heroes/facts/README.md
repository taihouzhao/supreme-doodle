# 原版事实库

一条事实一个文件。字段见 [`../PRD/04-validation.md`](../PRD/04-validation.md)。

当前没有锁定的 DOS 二进制哈希（`VERSION.json` 的 `fileHashSha256` 为空）。内容按公开攻略重建，状态为 `reconstructed-from-walkthrough`。有原版后再改 `verified-vs-original` 并写入哈希。
